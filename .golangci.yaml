version: "2"

linters:
  default: none
  enable:
    - staticcheck
    - unused

settings:
  errcheck:
    exclude-functions:
      - (io.Closer).Close
      - (net.Conn).Close
      - fmt.Fprint
      - fmt.Fprintf
      - fmt.Fprintln
